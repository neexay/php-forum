<?php

/*const HOST = "localhost";
const USERNAME = "root";
const PASSWORD = "root";
const DATABASE = "forum";


class reg_auth
{
    //login, password1, password2, email - всего 4 переменных
    //password1 - поле "пароль", а password2 - поле "повторите пароль"

    public function comparePasswords()
    {
        $password1 = $_GET['password1'];
        $password2 = $_GET['password2'];
        if ($password1 === $password2) {
            $final_password = $password1;
        } else {
            echo "<script> alert('Пароли не совпадают!') </script>";
            return 'registration.php';
        }
    }

    public function checkLoginAndPasswordLength()
    {
        $login = $_GET['login'];
        if (1==1) {
            echo 'да';
        }
    }
    *
}

(new reg_auth)->comparePasswords();
*/